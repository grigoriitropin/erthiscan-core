apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: erthiscan-gateway
  namespace: erthiscan
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      port: 80
      protocol: HTTP
