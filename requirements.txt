fastapi # Web framework for building the API
uvicorn # ASGI server to run the FastAPI application
asyncpg # Async PostgreSQL driver
sqlalchemy[asyncio] #ORM and database toolkit
alembic # Database migrations
psycopg[binary] # Sync PostgreSQL driver for migrations
httpx # HTTP client for collector lookups
google-auth # Verify Google ID tokens
requests # Required by google-auth transport
PyJWT # Issue signed access tokens
redis[hiredis] # Redis client with C parser for caching and JWT blacklist
aiokafka # Async Kafka client for event processing
unidecode # Transliterate non-ASCII text to ASCII for cross-script search
